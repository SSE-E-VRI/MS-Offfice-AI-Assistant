<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Tokens.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!--
        Reusable control styles (Phase 0.3 design system).

        These are all explicitly keyed (x:Key), not TargetType-implicit — merging this
        dictionary must not change the appearance of any existing control. Phase A wires
        specific controls to these styles as part of its visual design pass; for 0.3 they
        are available but unused by the existing ChatSidebar/SettingsWindow markup, which
        keeps its current per-control property values (now via token references — see
        ChatSidebar.xaml / SettingsWindow.xaml).

        The canonical "card" idiom below matches what's already in use in SettingsWindow.xaml:
        white surface, 8px corner radius, 1px BorderBrush border, 14px padding.
    -->

    <!-- Card: the standard elevated content container -->
    <Style x:Key="CardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="14"/>
    </Style>

    <!-- Badge: small pill-shaped label, e.g. action target/type tags on office action cards -->
    <Style x:Key="BadgeStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource AccentSurfaceBrush}"/>
        <Setter Property="CornerRadius" Value="3"/>
        <Setter Property="Padding" Value="4,1"/>
    </Style>

    <Style x:Key="BadgeTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource AccentTextBrush}"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>

    <!-- Chip: dismissible token, e.g. attachment chips -->
    <Style x:Key="ChipStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource PrimarySurfaceFaintBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBorderFaintBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Padding" Value="6,3"/>
    </Style>

    <!-- Buttons: base + semantic variants layered on top of it -->
    <Style x:Key="BaseButtonStyle" TargetType="Button">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource InkOnDarkBrush}"/>
    </Style>

    <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
        <Setter Property="Background" Value="{StaticResource SurfaceAltBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource InkSecondaryBrush}"/>
    </Style>

    <Style x:Key="SuccessButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
        <Setter Property="Background" Value="{StaticResource SuccessStrongBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource InkOnDarkBrush}"/>
    </Style>

    <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
        <Setter Property="Background" Value="{StaticResource DangerSurfaceBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
    </Style>

</ResourceDictionary>
